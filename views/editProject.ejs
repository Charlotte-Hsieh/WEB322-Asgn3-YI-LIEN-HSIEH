<!DOCTYPE html>
<html lang="en" data-theme="retro">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Project</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mx-auto px-4 py-10">
    <div class="hero bg-base-200 rounded-lg mb-8 py-10">
      <div class="hero-content text-center">
        <div class="max-w-2xl">
          <h1 class="text-4xl font-bold mb-4">Edit Project</h1>
          <p>Editing: <span class="font-semibold"><%= project.title %></span></p>
        </div>
      </div>
    </div>

    <form action="/solutions/editProject" method="POST" class="space-y-6 max-w-3xl mx-auto">
      <input type="hidden" name="id" value="<%= project.id %>">

      <div class="form-control">
        <label class="label">
          <span class="label-text">Title</span>
        </label>
        <input type="text" name="title" required class="input input-bordered"
               value="<%= project.title %>">
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text">Feature Image URL</span>
        </label>
        <input type="url" name="feature_img_url" required class="input input-bordered"
               value="<%= project.feature_img_url %>">
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text">Sector</span>
        </label>
        <select name="sector_id" required class="select select-bordered">
          <option value="">Select a sector</option>
          <option value="1" <%= (project.sector_id == 1) ? "selected" : "" %>>Land Sinks</option>
          <option value="2" <%= (project.sector_id == 2) ? "selected" : "" %>>Industry</option>
          <option value="3" <%= (project.sector_id == 3) ? "selected" : "" %>>Transportation</option>
          <option value="4" <%= (project.sector_id == 4) ? "selected" : "" %>>Electricity</option>
          <option value="5" <%= (project.sector_id == 5) ? "selected" : "" %>>Food, Agriculture, and Land Use</option>
        </select>
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text">Intro (short)</span>
        </label>
        <textarea name="intro_short" required class="textarea textarea-bordered"><%= project.intro_short %></textarea>
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text">Summary (short)</span>
        </label>
        <textarea name="summary_short" required class="textarea textarea-bordered"><%= project.summary_short %></textarea>
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text">Impact</span>
        </label>
        <textarea name="impact" required class="textarea textarea-bordered"><%= project.impact %></textarea>
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text">Original Source URL</span>
        </label>
        <input type="url" name="original_source_url" required class="input input-bordered"
               value="<%= project.original_source_url %>">
      </div>

      <div class="flex items-center justify-between mt-6">
        <button type="submit" class="btn btn-primary">Update Project</button>
        <a href="/solutions/deleteProject/<%= project.id %>" class="btn btn-error">
          Delete Project
        </a>
      </div>
    </form>
  </div>
</body>
</html>
